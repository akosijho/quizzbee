import 'package:flutter/material.dart';

class HGap04 extends StatelessWidget {
  const HGap04({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 04,
    );
  }
}

class HGap08 extends StatelessWidget {
  const HGap08({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 08,
    );
  }
}

class HGap12 extends StatelessWidget {
  const HGap12({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 12,
    );
  }
}

class HGap16 extends StatelessWidget {
  const HGap16({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 16,
    );
  }
}